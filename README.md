# quizPBJ

A simple Streamlit quiz application that loads questions from a CSV file.

The app lets you choose how many questions to attempt and displays your progress
with a built-in progress bar. After finishing the quiz it shows your score and
offers a button to restart.

## Running

Install dependencies and run the app:

```bash
pip install -r requirements.txt
streamlit run quiz.py
```
